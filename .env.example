# Copy this file to `.env.local` (for development) or `.env` (for production build)
# and replace the placeholder values with the credentials produced by Supabase.

VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=replace_with_anon_key
